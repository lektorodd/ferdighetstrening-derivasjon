<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <style>
      @media (prefers-color-scheme: dark) {
        .bg { fill: #1e293b; }
        .text { fill: #60a5fa; }
      }
      @media (prefers-color-scheme: light) {
        .bg { fill: #2563eb; }
        .text { fill: #ffffff; }
      }
    </style>
  </defs>
  <rect class="bg" width="100" height="100" rx="20"/>
  <text class="text" x="50" y="58" font-family="'Georgia', serif" font-size="48" font-style="italic" font-weight="bold" text-anchor="middle" dominant-baseline="middle">f'</text>
</svg>
